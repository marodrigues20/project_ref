@Library('10x-SharedLib@helm/v2.8') _

runPipelineRelease(
  primaryBranch: 'develop',
  repositoryName: 'tsys-proxy-batch',
  namespace: 'cards',
  alias: 'tsysproxybatch',
  targetBranch: "${params.targetBranch}"
)
